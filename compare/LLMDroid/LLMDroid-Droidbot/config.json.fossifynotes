{
  "AppName": "Fossify Notes",
  "Description": "Fossify Notes (org.fossify.notes). The app supports create a text note, create a checklist note, tick and untick checklist items, reorder or remove checklist items, switch between notes, rename a note, delete a note, undo and redo edits, search inside a note, lock a note with a PIN, pattern or fingerprint, unlock a protected note, export a note to a file, export all notes at once, import a note from a file, keep a note synchronised with a file on disk, share a note, print a note, add the note home screen widget.",
  "ApiKey": "ollama",
  "Model": "qwen2.5vl:7b",
  "BaseUrl": "http://127.0.0.1:11434/v1",
  "Tag": "FOSSIFYNOTES_SUPER_LOG",
  "TotalMethod": 0
}
