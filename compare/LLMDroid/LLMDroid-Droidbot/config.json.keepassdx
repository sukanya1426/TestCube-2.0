{
  "AppName": "KeePassDX",
  "Description": "KeePassDX (com.kunzisoft.keepass.libre). The app supports create a database, open an existing database, unlock a database with a key file, unlock a database with a hardware key (challenge-response), unlock with biometrics or the device credential, create an entry, generate a password, add custom fields to an entry, add an attachment to an entry, download or open an attachment, set an expiration date on an entry, configure a one-time password (OTP) token for an entry, copy a username or password to the clipboard, reveal a hidden password, open the entry URL in the browser, edit an entry, view an entry's history, duplicate an entry.",
  "ApiKey": "ollama",
  "Model": "qwen2.5vl:7b",
  "BaseUrl": "http://127.0.0.1:11434/v1",
  "Tag": "KEEPASSDX_SUPER_LOG",
  "TotalMethod": 111626
}
