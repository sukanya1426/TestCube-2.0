{
  "AppName": "Joplin",
  "Description": "Joplin (net.cozic.joplin). The app supports create a notebook, create a nested sub-notebook, rename or delete a notebook, create a note, create a to-do, mark a to-do as completed, convert a note to a to-do and back, set an alarm on a to-do, write in Markdown with live syntax highlighting, use the Markdown formatting toolbar, switch between the Markdown editor and the rich text (WYSIWYG) editor, switch between edit mode and the rendered viewer, attach a photo taken with the camera, attach an image or file from the device, record and attach audio, use voice typing to dictate note text, attach the current geolocation to a note, add tags to a note.",
  "ApiKey": "ollama",
  "Model": "qwen2.5vl:7b",
  "BaseUrl": "http://127.0.0.1:11434/v1",
  "Tag": "JOPLIN_SUPER_LOG",
  "TotalMethod": 0
}
