# Material Files

Package: `me.zhanghai.android.files`
Version documented: 1.7.4
Source: Official Material Files repository documentation and F-Droid listing

This app is explored automatically by TestCube and by LLMDroid. The list
below is the documented, user-facing capability set; it is context for the
tester, not a script to follow.

## What the app does

Material Files exposes 42 documented user-facing features, including browse local storage, create a new folder, create a new empty file, copy files and folders, move files and folders, rename an item.

## Capabilities

- Browse local storage
- Create a new folder
- Create a new empty file
- Copy files and folders
- Move files and folders
- Rename an item
- Delete items
- Select multiple items and use select all
- Search for files
- Sort the file list
- Remember the sort order per folder
- Switch between list and grid view
- Show or hide hidden files
- View and open an archive
- Extract an archive
- Create an archive
- Open a file with another app
- Share files
- Copy the path of a file
- View file properties
- Change file permissions, owner and SELinux context (root)
- View an image in the built-in viewer
- Edit a text file in the built-in editor
- Change the text file encoding in the editor
- Bookmark a directory
- Manage bookmark directories
- Add a shortcut to a folder on the home screen
- Connect to an FTP or FTPS server
- Connect to an SFTP server
- Connect to an SMB share
- Connect to a WebDAV server
- Edit or remove a saved server
- Start the built-in FTP server to share files with a computer
- Configure the FTP server settings
- Enable root access
- Show a "root directory" entry in the navigation
- Change the theme and colours
- Change the app language
- Configure the file list behaviour
- Configure the standard directories shown in the navigation drawer
- Configure the storage volumes shown
- Open the about screen and licences
