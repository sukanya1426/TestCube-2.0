# NoNonsense Notes

Package: `com.nononsenseapps.notepad`
Version documented: 7.2.6
Source: Official NoNonsense Notes repository documentation and F-Droid listing

This app is explored automatically by TestCube and by LLMDroid. The list
below is the documented, user-facing capability set; it is context for the
tester, not a script to follow.

## What the app does

NoNonsense Notes exposes 34 documented user-facing features, including create a task list, rename a task list, delete a task list, set the default list, create a task, edit a task.

## Capabilities

- Create a task list
- Rename a task list
- Delete a task list
- Set the default list
- Create a task
- Edit a task
- Mark a task as completed
- Show or hide completed tasks
- Delete all completed tasks in a list
- Create sub-tasks by indenting
- Reorder tasks manually
- Set a due date on a task
- Set a time-based reminder
- Set a repeating reminder
- Set a location-based reminder
- Remove a reminder
- Move a task to a different list
- Delete a task
- Sort a list
- Switch between lists by swiping
- View all tasks from all lists together
- Search tasks
- Share a task
- Create a task from text shared by another app
- Add the task list home screen widget
- Configure widget appearance
- Change the app theme
- Change the list and font sizes
- Enable synchronisation with a storage folder (SD card sync)
- Configure notification behaviour
- Back up the task database
- Restore the task database from a backup
- Configure the swipe gesture on list items
- View the about screen and licences
