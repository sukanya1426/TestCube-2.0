# Joplin

Package: `net.cozic.joplin`
Version documented: 3.6.21
Source: Official Joplin documentation (joplinapp.org) and in-app menus

This app is explored automatically by TestCube and by LLMDroid. The list
below is the documented, user-facing capability set; it is context for the
tester, not a script to follow.

## What the app does

Joplin exposes 45 documented user-facing features, including create a notebook, create a nested sub-notebook, rename or delete a notebook, create a note, create a to-do, mark a to-do as completed.

## Capabilities

- Create a notebook
- Create a nested sub-notebook
- Rename or delete a notebook
- Create a note
- Create a to-do
- Mark a to-do as completed
- Convert a note to a to-do and back
- Set an alarm on a to-do
- Write in Markdown with live syntax highlighting
- Use the Markdown formatting toolbar
- Switch between the Markdown editor and the rich text (WYSIWYG) editor
- Switch between edit mode and the rendered viewer
- Attach a photo taken with the camera
- Attach an image or file from the device
- Record and attach audio
- Use voice typing to dictate note text
- Attach the current geolocation to a note
- Add tags to a note
- Browse notes by tag
- Search notes
- Sort the note list
- Reorder notes manually
- Move a note to another notebook
- Duplicate a note
- Delete a note
- Restore notes from the trash
- Configure synchronisation with a remote target
- Run a synchronisation manually
- Set the automatic synchronisation interval
- Enable end-to-end encryption
- Enter a master key password to decrypt synced notes
- Publish/share a note (Joplin Cloud account required)
- Lock the app with a PIN or biometrics
- Change the application theme
- Change the editor and viewer font size
- Enable Markdown rendering plugins
- Install and manage Joplin plugins
- Configure note behaviour defaults
- Export a JEX/RAW backup or share note data
- Import notes from a file
- Send a note to another app
- Create a note from another app via the share sheet
- View note properties
- View the synchronisation status and log
- Check for application updates
