# Amaze File Manager

Package: `com.amaze.filemanager`
Version documented: 3.11.3
Source: Official Amaze File Manager repository documentation and F-Droid listing

This app is explored automatically by TestCube and by LLMDroid. The list
below is the documented, user-facing capability set; it is context for the
tester, not a script to follow.

## What the app does

Amaze File Manager exposes 46 documented user-facing features, including browse internal storage and the SD card, create a new folder, create a new empty file, copy files and folders, move (cut) files and folders, rename a file or folder.

## Capabilities

- Browse internal storage and the SD card
- Create a new folder
- Create a new empty file
- Copy files and folders
- Move (cut) files and folders
- Rename a file or folder
- Delete files and folders
- Select multiple items
- Search for files in the current folder
- Run a deep search across subfolders
- Sort the file list
- Switch between list and grid view
- Show or hide hidden files
- Open a file with an external app
- Share files
- View file properties
- Change file permissions (root)
- Compress files into an archive
- Extract an archive
- Browse inside an archive without extracting it
- Encrypt a file
- Decrypt a file
- Edit a text file in the built-in editor
- Add a folder to the bookmarks
- Edit or remove a bookmark
- Open a second tab / use the dual pane
- Set a folder as the home directory
- Open the app manager
- Uninstall an application from the app manager
- Back up an installed app's APK
- Start the built-in FTP server
- Configure the FTP server
- Connect to an SMB (Windows) share
- Connect over SFTP/SSH
- Connect to a cloud storage account
- Enable root explorer mode
- View recent files
- Browse files by category
- Change the theme
- Change the accent and colour scheme
- Configure behaviour settings
- Enable the encrypted folder/hidden files protection
- Add a shortcut to a folder on the home screen
- Copy a file path to the clipboard
- View storage usage
- Open the about screen and licences
