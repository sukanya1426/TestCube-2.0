# Markor

Package: `net.gsantner.markor`
Version documented: 2.16.1
Source: Official Markor repository documentation and F-Droid listing

This app is explored automatically by TestCube and by LLMDroid. The list
below is the documented, user-facing capability set; it is context for the
tester, not a script to follow.

## What the app does

Markor exposes 46 documented user-facing features, including browse the notebook folder, create a new plain text or Markdown document, create a document from a template, create a folder, edit a document in the text editor, switch between edit mode and rendered preview.

## Capabilities

- Browse the notebook folder
- Create a new plain text or Markdown document
- Create a document from a template
- Create a folder
- Edit a document in the text editor
- Switch between edit mode and rendered preview
- Apply Markdown formatting with the action toolbar
- Insert bullet, numbered and checkbox lists
- Insert a link
- Insert an image from the gallery or camera
- Record and insert an audio note
- Insert the current date or time
- Insert a table
- Undo and redo edits
- Search inside the open document
- Search across all files in the notebook
- Open the QuickNote file
- Open the To-Do file
- Manage a todo.txt file
- Edit a Zim Wiki page
- Edit AsciiDoc, Org-Mode, CSV and plain text files
- Preview a CSV file as a table
- Create a Markdown presentation
- Export the document as a PDF or print it
- Share the document as plain text or rendered HTML
- Rename a file or folder
- Move or copy files and folders
- Delete files and folders
- Select multiple files at once
- Mark a file or folder as a favourite
- Sort the file list
- Show or hide hidden files
- Jump to a specific folder path
- View recent and popular documents
- View file information
- Encrypt and open password-protected notes
- Open a file with another application
- Add a launcher shortcut to a document
- Add the Markor home screen widget
- Change the notebook (root) directory
- Change the theme and colours
- Change the editor font family and size
- Configure editor behaviour
- Configure the format action bar buttons
- Change the app language
- Review the changelog, help and licences
