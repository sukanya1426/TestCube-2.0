# Aegis Authenticator

Package: `com.beemdevelopment.aegis`
Version documented: 3.4.2
Source: Official Aegis repository documentation and F-Droid listing

This app is explored automatically by TestCube and by LLMDroid. The list
below is the documented, user-facing capability set; it is context for the
tester, not a script to follow.

## What the app does

Aegis Authenticator exposes 40 documented user-facing features, including create and encrypt the vault on first launch, unlock the vault with a password, unlock the vault with biometrics, add an entry by scanning a QR code, add an entry from a QR code image, add an entry manually.

## Capabilities

- Create and encrypt the vault on first launch
- Unlock the vault with a password
- Unlock the vault with biometrics
- Add an entry by scanning a QR code
- Add an entry from a QR code image
- Add an entry manually
- Edit an entry
- Set a custom icon for an entry
- Import an icon pack
- Copy a code to the clipboard
- Reveal a hidden code
- Generate the next HOTP code
- Search entries
- Sort the entry list
- Reorder entries manually
- Create a group
- Assign an entry to a group
- Filter the list by group
- Rename or delete a group
- Delete an entry
- Select and act on multiple entries
- Export the vault as an encrypted file
- Export the vault in plain text or as HTML
- Import a vault from Aegis or another authenticator
- Import entries by scanning a Google Authenticator transfer QR code
- Set or change the vault password
- Add a second unlock method (password slot)
- Configure automatic locking
- Set the lock timeout
- Block screenshots and screen recording
- Enable a panic trigger
- Enable automatic backups to a folder
- Enable Android system backups
- Change the theme
- Change the accent colour and dynamic colours
- Change the entry list view mode
- Configure code display and copy behaviour
- Highlight the entry whose code is about to expire
- Change the app language
- View the about screen and licences
