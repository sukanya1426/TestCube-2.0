# KeePassDX

Package: `com.kunzisoft.keepass.libre`
Version documented: 4.4.5
Source: Official KeePassDX documentation (keepassdx.com) and in-app menus

This app is explored automatically by TestCube and by LLMDroid. The list
below is the documented, user-facing capability set; it is context for the
tester, not a script to follow.

## What the app does

KeePassDX exposes 47 documented user-facing features, including create a database, open an existing database, unlock a database with a key file, unlock a database with a hardware key (challenge-response), unlock with biometrics or the device credential, create an entry.

## Capabilities

- Create a database
- Open an existing database
- Unlock a database with a key file
- Unlock a database with a hardware key (challenge-response)
- Unlock with biometrics or the device credential
- Create an entry
- Generate a password
- Add custom fields to an entry
- Add an attachment to an entry
- Download or open an attachment
- Set an expiration date on an entry
- Configure a one-time password (OTP) token for an entry
- Copy a username or password to the clipboard
- Reveal a hidden password
- Open the entry URL in the browser
- Edit an entry
- View an entry's history
- Duplicate an entry
- Move an entry to another group
- Delete an entry
- Create a group
- Edit, move or delete a group
- Use the recycle bin
- Change an entry or group icon
- Search the database
- Configure search behaviour
- Sort the entry list
- Use the KeePassDX autofill service
- Use the Magikeyboard to type credentials
- Save changes to the database
- Open a database in read-only mode
- Lock the database manually
- Configure automatic locking
- Configure clipboard timeout
- Block screenshots
- Change the master key of a database
- Change the database encryption settings
- Edit the database name, description and default username
- Configure history and compression settings
- Use entry templates
- Merge/synchronise changes from another copy of the database
- Save a copy of the database (Save as)
- Add a database to the recent list and pin it
- Change the app theme and appearance
- Change the app language
- Configure keyboard and form-filling behaviour
- View the about screen, contribution and licence information
